# Changelog 2026-03-17 #11 - API smoke success after bootstrap

## Як було
- Після migrate+seed потрібно було підтвердити, що API працює end-to-end.

## Що було зроблено
- Запущено API сервер (`npm run dev` у backend).
- Перевірено `GET /api/health`.
- Перевірено `POST /api/auth/login` з `core64admin`.
- Перевірено `GET /api/public`.
- Після тесту сервер коректно зупинено.

## Що це покращило, виправило, додало
- Підтвердило працездатність backend після DB bootstrap.
- Підтвердило, що авторизація повертає токен.
- Підтвердило, що публічний агрегований endpoint читає дані з БД.
- Закрило технічний ризик першого інтеграційного етапу.
