# Changelog

## [Unreleased] - 2024-03-18
### Added
- Added early container connectivity guard in `loadArtists` so artist cards are rendered only when `artists-list` exists and is connected to the DOM.