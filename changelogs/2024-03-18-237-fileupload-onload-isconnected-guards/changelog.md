# Changelog

## [Unreleased] - 2024-03-18
### Added
- Hardened `handleFileUpload` `FileReader.onload` path with `isConnected` checks for upload input, parent container, image URL input, and preview image before applying Base64 updates.