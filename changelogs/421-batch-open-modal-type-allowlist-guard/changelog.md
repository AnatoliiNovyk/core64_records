# 421 Batch - Open modal type allowlist guard

як було:

- `openModal` перевіряв лише, що `type` є непорожнім рядком, але не обмежував значення до підтримуваних типів.

що зроблено:

- Додано allowlist-guard: функція продовжує виконання тільки для `release`, `artist`, `event`.

що покращило/виправило/додало:

- Усунуто edge-case ризик відкриття модалки з невалідним типом та неконсистентним станом/контентом.
- Підвищено передбачуваність modal-flow без змін штатної поведінки.
