# Changelog

## [Unreleased] - 2024-03-18
### Added
- Added `avgLatencyEl.isConnected` guard in `applyAuditLatencyThresholds` before setting tooltip title, preventing attribute updates on detached latency indicator nodes.