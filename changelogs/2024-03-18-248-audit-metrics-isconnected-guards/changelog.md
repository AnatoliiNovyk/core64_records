# Changelog

## Added

Added `isConnected` guards in `loadAuditLogs` for audit metric nodes (`audit-last-updated`, `audit-last-latency`, `audit-avg-latency`, and nested avg text span) before asynchronous text updates.
