# 420 Batch - Open modal type input guard

- як було:
  - `openModal` одразу оновлював `editingId`/`editingType`, навіть якщо `type` потенційно невалідний.

- що зроблено:
  - Додано ранній guard на початку `openModal`: функція завершується, якщо `type` не рядок або порожній.

- що покращило/виправило/додало:
  - Усунуто edge-case ризик потрапляння невалідного `type` у глобальний modal-state.
  - Підвищено стабільність без зміни штатної поведінки для валідних викликів.
