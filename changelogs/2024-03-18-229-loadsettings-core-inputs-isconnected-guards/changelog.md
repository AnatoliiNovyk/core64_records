# Changelog

## Added

Added `isConnected` guards in `loadSettings` for `setting-title`, `setting-about`, `setting-mission`, and `setting-email` assignments to avoid writing values to detached settings inputs.
