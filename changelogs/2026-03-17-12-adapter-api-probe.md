# Changelog 2026-03-17 #12 - Adapter API probe for admin strict mode

## Як було
- Адмінка не мала окремого явного методу перевірки доступності API перед роботою.

## Що було зроблено
- У `data-adapter.js` додано метод `isApiAvailable()` з перевіркою `/health`.

## Що це покращило, виправило, додало
- Додало технічну основу для явного повідомлення в адмінці при недоступному API.
- Прибрало неоднозначність стартової діагностики (окрема перевірка замість непрямих помилок).
