# 2026-04-08-07 Public Sections Order Reverse Fix

## Як було
- У public reorder керованих секцій перед блоком About використовувався цикл у зворотному напрямку.
- Це фактично реверсило бажаний порядок секцій у DOM.
- Через це UI smoke падав з помилкою порядку (`artists,releases,events` замість очікуваного `events,releases,artists`).

## Що зроблено
- У [app.js](app.js) виправлено напрямок ітерації під час вставки керованих секцій перед About:
  - було: від останнього елемента до першого
  - стало: від першого до останнього
- Після правки повторно запущено `npm run ui-smoke`.

## Що покращило/виправило/додало
- Відновлено коректний порядок секцій на публічній сторінці згідно з налаштуваннями sortOrder.
- Усунено реверс-артефакт у видимих секціях та навігації.
- UI smoke тепер проходить успішно для сценарію reorder/hide секцій.
