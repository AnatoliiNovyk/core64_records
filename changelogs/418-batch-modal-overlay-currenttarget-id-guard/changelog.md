# 418 Batch - Modal overlay currentTarget id guard

як було:

- Клік-обробник оверлею модалки перевіряв `defaultPrevented`, connected-стан та збіг `target/currentTarget`, але не мав явної валідації id контейнера.

що зроблено:

- Додано guard: обробник завершується, якщо `currentTarget.id` не дорівнює `modal`.

що покращило/виправило/додало:

- Зменшено ризик небажаного закриття модалки у випадку неочікуваного перевикористання/ретаргетингу обробника.
- Посилено передбачуваність поведінки без зміни основного UX-флоу.
