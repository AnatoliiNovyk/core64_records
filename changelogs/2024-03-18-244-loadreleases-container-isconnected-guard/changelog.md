# Changelog

## [Unreleased] - 2024-03-18
### Added
- Added early container connectivity guard in `loadReleases` so release cards are rendered only when `releases-list` exists and is connected to the DOM.