# Changelog

## [Unreleased] - 2024-03-18
### Added
- Added `isConnected` guard in `animateSettingsUnsavedToastProgress` inside the `requestAnimationFrame` callback to prevent width updates on detached toast progress elements.