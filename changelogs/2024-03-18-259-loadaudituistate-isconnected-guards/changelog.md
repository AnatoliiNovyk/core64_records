# Changelog

## [Unreleased] - 2024-03-18
### Added
- Added `isConnected` guards in `loadAuditUiState` for restored audit controls (`search`, filters, date inputs, eco mode, limit, refresh interval) before assigning saved values.