# Changelog

## [Unreleased] - 2024-03-18
### Added
- Added `progressEl.isConnected` guard in `setSettingsUnsavedToastProgress` to skip style updates when the toast progress element is detached from the DOM.