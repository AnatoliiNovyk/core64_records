# Changelog 2026-03-17 #05 - Health smoke check

## Як було
- Міграція БД була заблокована через невалідні credential'и PostgreSQL.
- Потрібно було підтвердити, що backend каркас працює хоча б на `health`.

## Що було зроблено
- Запущено API сервер із папки `backend`.
- Виконано запит `GET http://localhost:3000/api/health`.
- Отримано коректну відповідь: `{"status":"ok","service":"core64-api",...}`.

## Що це покращило, виправило, додало
- Підтвердило, що Express-сервер та роутинг працюють коректно.
- Відокремило проблему: блокер тільки на етапі доступу до PostgreSQL.
- Додало впевненість, що після коректного `DATABASE_URL` міграція/seed можна довести до кінця без архітектурних правок.
