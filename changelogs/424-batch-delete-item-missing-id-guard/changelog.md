# 424 Batch - Delete item missing id guard

як було:

- `deleteItem` перевіряв валідність `type`, але не мав раннього виходу при відсутньому `id`.

що зроблено:

- Додано guard: функція завершується, якщо `id === null` або `id === undefined`.

що покращило/виправило/додало:

- Усунуто edge-case виклики видалення з невалідним ідентифікатором.
- Підвищено стабільність delete-flow без змін штатної логіки для валідних `id`.
