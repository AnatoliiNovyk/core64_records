# Changelog 2026-03-17 #03 - Migration attempt (auth failed)

## Як було
- Підготовлено `backend/.env` для запуску міграції PostgreSQL.

## Що було зроблено
- Запущено команду `npm run migrate` у `backend`.
- Отримано помилку PostgreSQL `28P01 (auth_failed)`.

## Що це покращило, виправило, додало
- Додало точну діагностику: проблема не в коді міграції, а в облікових даних БД.
- Звузило область перевірки до `DATABASE_URL`/пароля користувача `postgres`.
- Підготувало наступний крок: перевірка альтернативного connection string або актуальних cred'ів.
