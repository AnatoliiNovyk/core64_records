# 425 Batch - Edit item missing id guard

- як було:
  - `editItem` перевіряв валідність `type`, але не мав окремої перевірки на наявність `id`.

- що зроблено:
  - Додано ранній guard: функція завершується, якщо `id === null` або `id === undefined`.

- що покращило/виправило/додало:
  - Усунуто edge-case, коли некоректний виклик редагування міг відкрити модалку у режимі створення.
  - Підвищено передбачуваність edit-flow без змін штатної поведінки.
