# 438 Batch - Submit editingType allowlist guard

- як було:
  - Submit-обробник модалки перевіряв лише наявність `editingType`, але не валідовував його проти підтримуваного allowlist перед adapter-викликами.

- що зроблено:
  - Додано ранній guard у submit-flow: якщо `editingTypeAtSubmit` не є підтримуваним сутнісним типом, обробник завершується.

- що покращило/виправило/додало:
  - Усунуто edge-case виклики `create/update` з невалідним `type` при атипових змінах стану.
  - Посилено консистентність валідації типу в усіх entry-point цієї ділянки.
